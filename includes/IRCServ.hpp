#ifndef IRCSERV_HPP
#define IRCSERV_HPP

#include "main.hpp"

class IRCServ {

	private :
	
	public :
		IRCServ();
		IRCServ & operator=(IRCServ const & rhs);
		IRCServ(IRCServ const & obj);
		~IRCServ();

};

#endif // IRCSERV_HPP
